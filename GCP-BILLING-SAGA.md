# 我和 Google Cloud 的一百美元恩怨

> 一个 watchdog 的 bug，烧掉了 GCP 上最后一点信任。
>
> 发出来就发出来了，谷歌要全球追杀就追杀吧。

---

## 起因

一切开始得很平常。

我需要用 Gemini API 做大规模内容生成——每天几千篇辩论、分析、知识图谱的批量产出。Google 的 Gemini 模型确实强，尤其是那个上下文窗口，做长文本推理无可替代。

我选了 GCP（Google Cloud Platform），直接调 Gemini API（不是 Vertex AI，那条路更贵）。开了个新账号，绑了一张空借记卡。

前几周一切正常。API 调用平稳，账单可控，一个月也就烧了四五十美金。虽然比预期高，但还在可接受范围内。

## 崩溃

但真正让我炸掉的，不是 Gemini 的 API 价格。

是一个 watchdog。

我在服务器上写了一个 `watchdog.py`，用来监控 `burn_night.py`（夜间批量生成任务）的运行状态。crontab 每隔 5 分钟跑一次：

```
*/5 * * * * /usr/bin/python3 /home/ben/kunpengzhi-ai/scripts/watchdog.py --cron
```

脚本里有一个 `--cron` 参数。但 `main()` 函数——从来没读过 `sys.argv`。

是的，你没看错。我写了参数解析的架子，加了 flag，挂在 crontab 里传了 `--cron`，但代码里实际上没有一行检查这个参数。每次 cron 触发，脚本都无视 `--cron`，直接进入主逻辑：启动 `burn_night.py`，然后进入一个永不退出的监控循环。

每 5 分钟一个进程。12 个小时。270 个堆积的 watchdog.py 实例。

每个实例都在调 Gemini API。

## 账单

等我发现的时候，CPU 负载 55+（4 核机器），内存剩 161MB，僵尸进程 134 个。

`watchdog.py` 占了一屏幕：

```
$ ps aux | grep watchdog | wc -l
270
```

Google Cloud 的控制台打开的时候，我沉默了几秒。

账单显示已经超了 100 美金，还在涨。

那个月总共烧了大概 150 刀——本来四五十就够了，多出来的一百是 watchdog 的狂欢。

## 退款

我试了 Google Cloud 的客服渠道——所谓的 "ATM channel"，提交了退款申请。

退了部分，扣了手续费。最终到手的不到五十。

但 GCP 的计费系统有个特点：它不太会停你的 API。哪怕账单逾期，服务通常还继续跑。这个"宽容"的设计，在这次成了灾难的放大器——270 个进程同时调 API，没有熔断，没有限流，没有终止。

## 收场

这个 GCP 账号是用 throwaway Google 账号开的，绑的空借记卡。

Google 的坏账追讨门槛通常在 500 美金以上，而且需要真实的身份信息。我这 100 刀，在他们那儿大概排不上号。

所以我做了个决定：**弃号**。

不是赖账——说实话我申请退款的时候是诚心想解决的。但整个过程的体验让我意识到一个事实：在 Google Cloud 的系统里，一个个人开发者是无足轻重的。没有电话，没有人工，没有紧急通道。你的服务在他们看来就是一行行数字——活跃度、ARPU、流失率。

而在代码的层面，我的疏忽才是元凶。一个没被读取的 `sys.argv`，12 小时烧掉了一百美金。

## 代价

那 270 个 watchdog 实例产生的大约 1562 篇 AI 内容——分析、辩论、知识图谱、交叉引用、播客脚本——全部保存在这个仓库的 `debate-logs/batch-2026-06-05/` 下。总共 1.7MB，来自 R2 的归档。

我本来想过把它们全部删掉，眼不见为净。

但仔细想了想：这些是真实工作的产出，每一篇背后都有推理链、有迭代、有逻辑结构。花掉的 credit 已经花了，如果连产物都删掉，那这一百刀就真的白烧了。

所以它们还在。

## 教训

1. **CLI 参数不是装饰品**——写了 `--cron` 就在代码里检查它。argparse 三行代码的事。
2. **任何调付费 API 的脚本都必须有熔断器**——进程数上限、日消费上限、意外行为自动终止。这三个缺一不可。
3. **系统负载不撒谎**——如果你的机器 CPU 55+、内存见底、僵尸进程遍地，先看 `ps aux`，别急着调配置。
4. **信任 Google Cloud 的"宽容"是一种傲慢**——它不会保护你，你得自己保护自己。

---

*2026 年 6 月 5 日*

*谨以此文纪念那个被 watchdog 吃掉的周末。*

*这个仓库里的 AI 生成内容——就是那 1562 篇、1.7MB 的 debate-logs——它们的来历，就是这个故事。*
