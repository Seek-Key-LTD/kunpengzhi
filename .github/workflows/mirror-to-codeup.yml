name: Mirror to Codeup (云效)

on:
  push:
    branches: [main]

jobs:
  mirror:
    runs-on: self-hosted
    steps:
      - name: Sync GitHub → Codeup
        run: bash /mnt/workspace/scripts/sync-codeup.sh
